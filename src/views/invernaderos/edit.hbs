<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Invernadero - Sistema de Riego IoT</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: '#10B981',
                        secondary: '#3B82F6',
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="/js/theme.js"></script>
    <script src="/js/main.js"></script>
</head>
<body class="h-full bg-gray-50 dark:bg-gray-900 transition-colors duration-200">
    {{> navbar}}
    
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8">
        <div class="max-w-2xl mx-auto">
            <div class="flex items-center gap-3 sm:gap-4 mb-6 sm:mb-8">
                <a href="/invernaderos" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 transition-colors">
                    <i class="fas fa-arrow-left text-lg sm:text-xl"></i>
                </a>
                <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white">Editar Invernadero</h1>
            </div>
            
            <div class="bg-white dark:bg-gray-800 rounded-lg sm:rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 p-4 sm:p-6">
                <form action="/invernaderos/{{invernadero.id}}" method="POST" class="space-y-4 sm:space-y-6">
                    <div>
                        <label for="descripcion" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Descripción</label>
                        <textarea id="descripcion" name="descripcion" rows="3" class="w-full rounded-lg border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white focus:ring-green-500 focus:border-green-500 shadow-sm text-sm sm:text-base">{{invernadero.descripcion}}</textarea>
                    </div>
                    
                    <div>
                        <label for="planta_id" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Planta Asignada</label>
                        <select id="planta_id" name="planta_id" class="w-full rounded-lg border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white focus:ring-green-500 focus:border-green-500 shadow-sm text-sm sm:text-base">
                            <option value="">Seleccionar planta...</option>
                            {{#each plantas}}
                            <option value="{{this.id}}" {{#if (eq this.id ../invernadero.planta_id)}}selected{{/if}}>{{this.nombre}}</option>
                            {{/each}}
                        </select>
                    </div>
                    
                    <div class="flex flex-col-reverse sm:flex-row justify-end gap-2 sm:gap-3 pt-4">
                        <a href="/invernaderos" class="w-full sm:w-auto text-center px-4 py-2.5 sm:py-2 border border-gray-300 dark:border-gray-600 rounded-lg text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 font-medium transition-colors text-sm sm:text-base">
                            Cancelar
                        </a>
                        <button type="submit" class="w-full sm:w-auto px-4 py-2.5 sm:py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors shadow-sm text-sm sm:text-base">
                            Guardar Cambios
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
