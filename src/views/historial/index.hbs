<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Historial de Sensores - {{invernadero.descripcion}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: '#10B981',
                        secondary: '#3B82F6',
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="/js/theme.js"></script>
    <script src="/js/main.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="h-full bg-gray-50 dark:bg-gray-900 transition-colors duration-200">
    {{> navbar}}
    
    <div class="container mx-auto px-4 py-8">
        <div class="flex flex-col md:flex-row justify-between items-center mb-8 gap-4">
            <div>
                <a href="/invernaderos/{{invernadero.id}}" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 transition-colors flex items-center gap-2 mb-2">
                    <i class="fas fa-arrow-left"></i> Volver al Invernadero
                </a>
                <h1 class="text-3xl font-bold text-gray-900 dark:text-white flex items-center gap-3">
                    <i class="fas fa-history text-purple-500"></i> Historial de Sensores
                </h1>
                <p class="text-gray-600 dark:text-gray-400 mt-1">Registros automáticos de {{invernadero.descripcion}}</p>
            </div>
            
            <div class="flex bg-gray-200 dark:bg-gray-700 rounded-lg p-1">
                <a href="/invernaderos/{{invernadero.id}}/historial" class="px-4 py-2 rounded-md bg-white dark:bg-gray-600 text-gray-900 dark:text-white shadow-sm font-medium text-sm transition-all">
                    Sensores
                </a>
                <a href="/invernaderos/{{invernadero.id}}/historial?tipo=acciones" class="px-4 py-2 rounded-md text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white font-medium text-sm transition-all">
                    Acciones
                </a>
            </div>
        </div>

        <!-- Gráfico (Placeholder) -->
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 p-6 mb-8">
            <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-4">Tendencias (Últimas 24h)</h3>
            <div class="h-64 flex items-center justify-center bg-gray-50 dark:bg-gray-700/30 rounded-lg border border-dashed border-gray-300 dark:border-gray-600">
                <p class="text-gray-500 dark:text-gray-400">Gráfico de temperatura y humedad aquí</p>
                <!-- Aquí iría un canvas de Chart.js -->
            </div>
        </div>

        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-gray-50 dark:bg-gray-700/50 border-b border-gray-100 dark:border-gray-700">
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Fecha y Hora</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Temperatura</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Humedad</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Estado Riego</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100 dark:divide-gray-700">
                        {{#each historial}}
                        <tr class="hover:bg-gray-50 dark:hover:bg-gray-700/30 transition-colors">
                            <td class="p-4 text-gray-900 dark:text-white">
                                {{formatDate this.fecha_registro}}
                            </td>
                            <td class="p-4">
                                <span class="font-medium text-orange-600 dark:text-orange-400">{{this.temp_registrada}}°C</span>
                            </td>
                            <td class="p-4">
                                <span class="font-medium text-blue-600 dark:text-blue-400">{{this.hum_registrada}}%</span>
                            </td>
                            <td class="p-4">
                                {{#if this.riego_activado}}
                                    <span class="px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400">
                                        Activado
                                    </span>
                                {{else}}
                                    <span class="px-2 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300">
                                        Inactivo
                                    </span>
                                {{/if}}
                            </td>
                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="4" class="p-8 text-center text-gray-500 dark:text-gray-400">
                                <i class="fas fa-history text-4xl mb-3 text-gray-300 dark:text-gray-600 block"></i>
                                No hay registros disponibles
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
