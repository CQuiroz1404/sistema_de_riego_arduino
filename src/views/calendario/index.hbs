<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calendario de Riego - {{invernadero.descripcion}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: '#10B981',
                        secondary: '#3B82F6',
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="/js/theme.js"></script>
    <script src="/js/main.js"></script>
</head>
<body class="h-full bg-gray-50 dark:bg-gray-900 transition-colors duration-200">
    {{> navbar}}
    
    <div class="container mx-auto px-4 py-8">
        <div class="flex flex-col md:flex-row justify-between items-center mb-8 gap-4">
            <div>
                <a href="/invernaderos/{{invernadero.id}}" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 transition-colors flex items-center gap-2 mb-2">
                    <i class="fas fa-arrow-left"></i> Volver al Invernadero
                </a>
                <h1 class="text-3xl font-bold text-gray-900 dark:text-white flex items-center gap-3">
                    <i class="fas fa-calendar-alt text-blue-500"></i> Calendario de Riego
                </h1>
                <p class="text-gray-600 dark:text-gray-400 mt-1">Programación para {{invernadero.descripcion}}</p>
            </div>
            <a href="/invernaderos/{{invernadero.id}}/calendario/create" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium transition-colors shadow-sm flex items-center gap-2">
                <i class="fas fa-plus"></i> Nuevo Evento
            </a>
        </div>

        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-gray-50 dark:bg-gray-700/50 border-b border-gray-100 dark:border-gray-700">
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Semana</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Hora Inicio</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Hora Fin</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300">Acción</th>
                            <th class="p-4 font-semibold text-gray-600 dark:text-gray-300 text-right">Opciones</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100 dark:divide-gray-700">
                        {{#each calendario}}
                        <tr class="hover:bg-gray-50 dark:hover:bg-gray-700/30 transition-colors">
                            <td class="p-4 text-gray-900 dark:text-white font-medium">
                                {{this.semana.nombre}}
                            </td>
                            <td class="p-4 text-gray-600 dark:text-gray-300">
                                {{this.hora_inicial}}
                            </td>
                            <td class="p-4 text-gray-600 dark:text-gray-300">
                                {{this.hora_final}}
                            </td>
                            <td class="p-4">
                                <span class="px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400">
                                    Riego
                                </span>
                            </td>
                            <td class="p-4 text-right">
                                <form action="/invernaderos/{{../invernadero.id}}/calendario/{{this.id}}/delete" method="POST" class="inline-block" onsubmit="return confirm('¿Estás seguro de eliminar este evento?');">
                                    <button type="submit" class="text-red-500 hover:text-red-700 transition-colors p-2">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="5" class="p-8 text-center text-gray-500 dark:text-gray-400">
                                <i class="fas fa-calendar-times text-4xl mb-3 text-gray-300 dark:text-gray-600 block"></i>
                                No hay eventos programados
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
